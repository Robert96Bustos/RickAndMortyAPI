---
// interface Props {
//     id: number
//     name: string
//     image: string
//     status: string
//     species: string
// }

// const { status, species, image, id, name } = Astro.props
// const CharacterStatus = status ? "Alive" : "Dead"

const {personaje} = Astro.props
console.log(personaje)
const {id,name, image} = personaje
---

<!-- <a 
    href=`/character/${id}`
    class="rounded-lg border shadow-md bg-gray-800 border-gray-700 
    hover:scale-105 hover:bg-gray-700 hover:border-gray-500 transition flex flex-col"
>
    <picture class="flex justify-center p-4">
        <img class="mb-5 rounded-lg" src={image} alt={`Patch for launch ${id}`} />
    </picture>

    <header class="p-4 flex-grow">
        <span class:list= {[
                "text-xs font-semibold mr-2 px-2,5 py-0.5 rounded",
                {
                    "bg-green-400 text-green-900": Alive,
                    "bg-red-400 text-red-900": !Alive
                }
            ]}
            >{CharacterStatus}
        </span>
        
        <h2 class="my-2 text-2xl font-bold tracking-tight text-white">
            {name}
        </h2>

        <h4 class="my-2 text-1xl font-bold tracking-tight text-white">
            Specie: {species}
        </h4>
    </header>
</a> -->
<div class="personaje">
    <h1>{name}</h1>
    <img src={image} alt="">
</div>